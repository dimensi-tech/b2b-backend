section.section
  .section-header
    h1 = t('.page_header')
    = render 'shared/breadcrumb'
  .section-body
    h2.section-title = @merchant.name
    p.section-lead
      = "Data ID : #{@merchant.id}"
    .row
      .col-12
        .card
          .card-header
            h4 = t('.details')
          .card-body
            .list-unstyled.list-unstyled-border.mt-4
              .media
                .media-icon
                  i.far.fa-circle
                .media-body
                  h6 = t('.name')
                  p = @merchant.name
              .media
                .media-icon
                  i.far.fa-circle
                .media-body
                  h6 = t('.description')
                  p = @merchant.description
  .buttons
    = link_to t('buttons.edit'), edit_merchant_path(@merchant), class: 'btn btn-warning'
    = link_to t('buttons.back'), merchants_path(@merchant), class: 'btn'
