section.section
  .section-header
    h1 = t('.page_header')
    .section-header-breadcrumb
      .breadcrumb-item.active
        a href="#"  Dashboard
      .breadcrumb-item Credits
  .section-body
    - if notice
      .alert.alert-success.alert-dismissible.show.fade
        .alert-body
          button.close data-dismiss="alert"
            span &times;
          = notice

    h2.section-title = @facility.name
    p.section-lead
      = "Data ID : #{@facility.id}"
    .row
      .col-12
        .card
          .card-header
            h4 = t('.details')
          .card-body
            .list-unstyled.list-unstyled-border.mt-4
              .media
                .media-icon
                  i.far.fa-circle
                .media-body
                  h6 = t('.package_id')
                  p = @facility.package_id
              .media
                .media-icon
                  i.far.fa-circle
                .media-body
                  h6 = t('.name')
                  p = @facility.name
              .media
                .media-icon
                  i.far.fa-circle
                .media-body
                  h6 = t('.description')
                  p = @facility.description
              .media
                .media-icon
                  i.far.fa-circle
                .media-body
                  h6 = t('.icon')
                  p = @facility.icon                  
  .buttons
    = link_to 'Edit', edit_facility_path(@facility), class: 'btn btn-warning'
    = link_to 'Back', facilities_path(@facility), class: 'btn'
