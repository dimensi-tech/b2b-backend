section.section
  .section-header
    h1 = t('.page_header')
    = render 'shared/breadcrumb'
  .section-body
    h2.section-title = @facility.name
    p.section-lead
      = "Data ID : #{@facility.id}"
    .row
      .col-12
        .card
          .card-header
            h4 = t('.details')
          .card-body
            .list-unstyled.list-unstyled-border.mt-4
              .media
                .media-icon
                  i.far.fa-circle
                .media-body
                  h6 = t('.package_id')
                  p = @facility.package.name
              .media
                .media-icon
                  i.far.fa-circle
                .media-body
                  h6 = t('.name')
                  p = @facility.name
              .media
                .media-icon
                  i.far.fa-circle
                .media-body
                  h6 = t('.icon')
                  p = @facility.icon
              .media
                .media-icon
                  i.far.fa-circle
                .media-body
                  h6 = t('.description')
                  p = @facility.description
  .buttons
    = link_to t('buttons.edit'), edit_facility_path(@facility), class: 'btn btn-warning'
    = link_to t('buttons.back'), facilities_path, class: 'btn'
